import React from "react";
import Navigation from "./Config/MainNavigation";
function App() {
  // const userinfo = localStorage.getItem("userinfo");
  // return <>{userinfo ? <Main /> : <Login />}</>;
  return <Navigation />;
}

export default App;
